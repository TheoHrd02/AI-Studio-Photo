/**
 * Product demo configuration.
 * Replace with a dedicated product walkthrough video when available.
 */

import { firstVideoUrl } from '~/config/hero.config'

/** URL of the embedded product demo video */
export const productDemoVideoUrl = firstVideoUrl
