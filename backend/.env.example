PORT=8080
GIN_MODE=release
# APP_ENV=production (default) — use APP_ENV=development for local debug
# CORS_ORIGINS: comma-separated explicit origins only (no wildcards)
CORS_ORIGINS=https://aistudiophoto.com,https://www.aistudiophoto.com

# OpenAI Configuration (Chatbot Support)
OPENAI_API_KEY=sk-your-openai-api-key
ASSISTANT_ID=asst_your_assistant_id
